<div class="container mt-5">
  <div class="card shadow-sm border-0">
    <div class="card-body">
      <h1 class="card-title text-center fw-bold mb-4"><%= @restaurant.name %></h1>

      <ul class="list-group list-group-flush">
        <li class="list-group-item">
          <i class="bi bi-geo-alt-fill"></i> <strong>Address:</strong> <%= @restaurant.address %>
        </li>
        <li class="list-group-item">
          <i class="bi bi-telephone-fill"></i> <strong>Phone:</strong> <%= @restaurant.phone_number %>
        </li>
        <li class="list-group-item">
          <i class="bi bi-tag-fill"></i> <strong>Category:</strong>
          <span class="badge bg-primary"><%= @restaurant.category.capitalize %></span>
        </li>
      </ul>

      <div class="text-center mt-4">
        <%= link_to "⬅ Back to Restaurants", restaurants_path, class: "btn btn-outline-secondary me-2" %>
        <%= link_to "📝 Add a Review", new_restaurant_review_path(@restaurant), class: "btn btn-success" %>
      </div>
    </div>
  </div>

  <div class="mt-5">
    <h3>📢 Reviews</h3>
    <% if @restaurant.reviews.any? %>
      <ul class="list-group">
        <% @restaurant.reviews.each do |review| %>
          <li class="list-group-item">
            <p><strong>⭐ Rating:</strong> <%= review.rating %>/5</p>
            <p><%= review.content %></p>
          </li>
        <% end %>
      </ul>
    <% else %>
      <p class="text-muted">No reviews yet. Be the first to leave one! 😊</p>
    <% end %>
  </div>
</div>
